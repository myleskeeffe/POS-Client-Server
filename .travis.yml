language: node_js
node_js:
  - node
before_install:
  - npm i -g npm@latest
install:
  - npm install
script:
  - npm run build --prod
notifications:
  email:
    on_success: never # default: change
    on_failure: always # default: always
